using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameMaster : MonoBehaviour
{
    public bool ChairSnapZoneActive = true;
    public bool BarrellSnapZoneActive = false;
    public GameObject ChairSnapZone;
    public GameObject BarrellSnapZone;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
